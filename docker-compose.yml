services:
  prod:
    image: sergeysazonov/bazhov-dictionary:${TAG}
    container_name: bazhov-dictionary-prod
    ports:
      - "4000:3000"
    restart: always
    env_file:
      - .env
    environment:
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
